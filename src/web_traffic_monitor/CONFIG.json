{

    "db_path":"{{data.Dir}}/{{package.name}}.db",
    "db_connection_module": "sqlite",

    "db_conn_kwargs" : {
        "db_path": "{{db_path}}",
        "connection_module":"{{db_connection_module}}"       
    },

    "db_get_conn_kwargs": {
        "check_same_thread": false
    }
    
}

