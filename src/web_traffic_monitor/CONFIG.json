{

    "db_path":"{{data.Dir}}/{R{package.name}R}.db",
    "db_connection_module": "sqlite",

    "db_conn_kwargs" : {
        "db_path": "{R{db_path}R}",
        "connection_module":"{R{db_connection_module}R}"       
    },

    "db_get_conn_kwargs": {
        "check_same_thread": false
    }
    
}

